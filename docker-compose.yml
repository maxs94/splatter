services:
  splatter:
    build: .
    ports:
      - "3000:3000"
    environment:
      BOTS: "3"        # default number of bots in a new lobby (the leader can change it)
      BOT_SKILL: "0.4" # 0 = hopeless, 1 = sharp
    restart: unless-stopped
